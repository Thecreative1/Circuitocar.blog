{
  "name": "circuitocar-blog",
  "version": "1.0.0",
  "description": "Circuito Car Blog — Eleventy static site",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "dev": "eleventy --serve --watch",
    "local": "cross-env BASEPATH= eleventy --serve --port 4000"
  },
  "dependencies": {
    "@remotion/tailwind": "^4.0.464",
    "cross-env": "^7.0.3",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^2.0.0",
    "@playwright/test": "^1.60.0"
  }
}
